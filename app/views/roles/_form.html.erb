<%= form_with(model: role, local: true) do |form| %>
  <% if role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(role.errors.count, "error") %> prohibited this role from being saved:</h2>

      <ul>
      <% role.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :role_description %>
  </div>

  <div class="field">
    <%= form.label :dateCreation %>
    <%= form.date_select :dateCreation, id: :role_dateCreation %>
  </div>

  <div class="field">
    <%= form.label :dateUpdate %>
    <%= form.date_select :dateUpdate, id: :role_dateUpdate %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
